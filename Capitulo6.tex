\section{Implementación del Sistema y Pruebas}

En este capítulo se presenta el entorno técnico en el que se llevó a cabo la implementación del \textit{Nez-Daemon Watcher}, así como las herramientas y procedimientos empleados durante su fase de validación. El propósito es ofrecer una descripción clara de los componentes de hardware y software utilizados, además de documentar las pruebas realizadas para confirmar que el sistema opera conforme a los requerimientos establecidos.

\subsection{Hardware Utilizado}

Para el desarrollo y las pruebas del proyecto se optó por utilizar un equipo de cómputo personal, en lugar de solicitar uno al CINVESTAV. Esta elección respondió principalmente a la necesidad de contar con un entorno de trabajo disponible de manera continua, tanto en la oficina como fuera de ella. El sistema se implementó y evaluó en una computadora portátil HP Victus, cuyo uso permitió mantener un flujo de trabajo estable y replicable durante el proceso de desarrollo.

A continuación se presentan las especificaciones del hardware empleado, con el fin de contextualizar las condiciones bajo las cuales se ejecutaron las pruebas y de proporcionar una referencia para cualquier futura réplica o comparación de resultados:

\begin{itemize}
    \item \textbf{Procesador: Intel Core i5 de 12.ª generación.} Este procesador de múltiples núcleos fue fundamental para ejecutar de manera fluida y simultánea el entorno de desarrollo, el motor de Docker y los múltiples contenedores que simulaban el ecosistema de MictlanX.
    
    \item \textbf{Memoria RAM: 16 GB.} La capacidad de la memoria RAM fue crucial para gestionar la carga de trabajo del sistema. Permitió que el sistema operativo, el entorno de desarrollo, el demonio de Docker y todos los servicios contenedorizados (Peers, Router, SPM) operaran de forma concurrente sin degradar el rendimiento.
    
    \item \textbf{Almacenamiento: 500 GB SSD M.2.} El uso de un disco de estado sólido (SSD) con interfaz M.2 ofreció altas velocidades de lectura y escritura. Esto impactó positivamente en la eficiencia del desarrollo, reduciendo los tiempos de arranque de los contenedores y acelerando las operaciones de entrada/salida del sistema de archivos, un aspecto clave para las pruebas del \textit{Watcher}.
    
    \item \textbf{GPU: NVIDIA GeForce GTX 1650 (4 GB VRAM).} Si bien el servicio \textit{Watcher} no hace un uso intensivo de la unidad de procesamiento gráfico (GPU), su presencia en el equipo de desarrollo asegura la capacidad de ejecutar futuras cargas de trabajo relacionadas con el análisis de datos o el aprendizaje automático, que son casos de uso comunes en la plataforma \textit{Nez}.
\end{itemize}

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.8\textwidth]{pc.png} % Asegúrate de que 'pc.jpg' esté en la misma carpeta o especifica la ruta
    \caption{Ordenador portátil HP Victus utilizado para el desarrollo y pruebas del sistema.}
    \label{fig:hp_victus}
\end{figure}

El uso de este equipo demostró que, gracias a la contenedorización, es posible simular y desarrollar para sistemas distribuidos complejos sin la necesidad inmediata de una infraestructura de hardware a gran escala, validando la portabilidad y eficiencia del software desarrollado.

\subsection{Software Utilizado}
La selección del software se centró en tecnologías modernas, de código abierto y alineadas con los requerimientos de un sistema distribuido, asíncrono y portable.

\begin{itemize}
    \item \textbf{Visual Studio Code \cite{Microsoft:Concepto}:} Fue el entorno de desarrollo integrado (IDE) principal para la escritura y depuración del código. Su naturaleza ligera, junto con un potente ecosistema de extensiones, lo convirtieron en la herramienta ideal. Se utilizaron extensiones clave para el desarrollo con Python (ofreciendo autocompletado, \textit{linting} y formato de código) y para la integración con Docker, que permitió gestionar los contenedores, visualizar sus estados y acceder a sus registros directamente desde el editor.

    \item \textbf{Python 3.12:} Fue el lenguaje de programación elegido para desarrollar el servicio \textit{Nez-Daemon Watcher}. Su elección se justifica por su robusto soporte para la programación asíncrona a través de la biblioteca \texttt{asyncio} \cite{Python:2024:asyncio}, fundamental para gestionar operaciones de red y de sistema de archivos de manera concurrente y eficiente. Además, se utilizó un ecosistema de librerías clave, entre las que destacan:
    \begin{itemize}
        \item \texttt{watchdog}: Para el monitoreo continuo y no bloqueante del sistema de archivos.
                    \item \texttt{httpx} \cite{HTTPX:2024:docs}: Un cliente HTTP asíncrono moderno, utilizado para la comunicación resiliente con la API REST del \textit{Router} de MictlanX.
                    \item \texttt{python-dotenv} \cite{Dotenv:2024:pypi}: Para la gestión de la configuración a través de variables de entorno, adhiriéndose a los principios de la metodología \textit{12-Factor App}.
                \end{itemize}
                
                \item \textbf{Docker \cite{Docker:2024:overview, merkel2014docker}:} Es una pieza central de la arquitectura. Se utilizó para empaquetar el \textit{Nez-Daemon Watcher} y todas sus dependencias en una imagen de contenedor portátil. Asimismo, se empleó para desplegar el ecosistema completo de MictlanX en el entorno de desarrollo. El uso de Docker garantiza la consistencia entre los entornos de desarrollo y producción, simplifica el despliegue y aísla los servicios, cumpliendo con el requerimiento no funcional de portabilidad.

\item \textbf{MictlanX Virtual Storage Space (VSS):} Actuó como el sistema de almacenamiento distribuido de destino. Para las pruebas, una instancia completa del VSS, incluyendo su \textit{Router} y múltiples nodos \textit{Peer}, fue desplegada localmente mediante contenedores Docker. Esto permitió validar la correcta interacción del \textit{Watcher} con la API de MictlanX para la subida, replicación y verificación de archivos.
\end{itemize}

\subsection{Elementos a Evaluar}

Durante las fases de desarrollo y pruebas se evaluó la funcionalidad del cliente implementado en C++ \cite{Stroustrup:2013:cpp}. El análisis se centró en verificar su comportamiento al interactuar con los datos alojados en el Espacio de Almacenamiento Virtual (VSS) de MictlanX, así como en revisar que cumpliera adecuadamente con las operaciones previstas dentro del flujo de trabajo.

Se comprobó que el cliente permite consultar archivos individuales y también manejar estructuras más amplias, como carpetas completas que contienen expedientes de pacientes. Esto facilitó revisar distintos escenarios de uso y confirmar que el sistema es capaz de procesar información en diferentes niveles de organización.

También se evaluaron las operaciones básicas de transferencia de datos, incluyendo la subida y descarga de archivos hacia y desde la nube. Estas pruebas se realizaron para identificar posibles fallos, validar el manejo de errores y asegurar que la información pudiera ser transferida sin modificaciones no deseadas.

Hasta ahora los resultados obtenidos permitieron identificar el comportamiento del cliente bajo condiciones de uso típicas y detectar áreas donde podrían requerirse ajustes o mejoras en futuras iteraciones del sistema.

\newpage

\subsection{Pruebas}
La validación del sistema se realizo en dos fases principales. La primera se enfoco en evaluar el servicio \textit{Watcher} de forma aislada, mientras que la segunda consistio en revisar su funcionamiento al integrarse con los componentes responsables de la comunicacion externa.
En la fase inicial, el \textit{Watcher} se probo de manera independiente con el proposito de confirmar su funcion principal: detectar nuevos archivos y enviarlos automaticamente al VSS de MictlanX. Para ello, el servicio se ejecuto dentro de un contenedor Docker con el fin de contar con un entorno controlado y reproducible durante las pruebas. 

La supervisacion del comportamiento en tiempo real se realizo mediante el comando \texttt{docker logs -f <container\_id>}, lo cual permitio observar la secuencia de eventos desde la deteccion de un archivo hasta la confirmacion de su carga.

El analisis de estas trazas facilito la identificacion de fallos y la revision del flujo interno del servicio. Entre los problemas detectados se encontraron errores de conexion con la API de MictlanX, interrupciones temporales en las solicitudes y comportamientos inesperados asociados con la logica de reintentos. Esta informacion permitio ajustar la implementacion y verificar que las correcciones aplicadas resolvieran los incidentes sin generar efectos secundarios.

\begin{figure}[h!]
    \centering
    \includegraphics[width=\textwidth]{Diagramas UML/ss_inicio.png} % Reemplazar con la imagen de los logs
    \caption{Captura de los registros al iniciar el servcio}
    \label{fig:watcher_logs}
\end{figure}

La Figura \ref{fig:watcher_logs} muestra el momento exacto del inicio y preparación exitosa del servicio NEZ-DAEMON WATCHER, centrado en la integración con MictlanX. El proceso comienza con el reconocimiento y la configuración de la conexión al router de MictlanX (host.docker.internal:60666), seguido de una verificación de salud exitosa del servicio MictlanX (HTTP/1.1 200 OK). Una vez confirmada la disponibilidad del servicio de backend, el daemon inicia sus componentes principales: arranca 5 tareas worker, activa el observador Watchdog (para monitorizar el sistema de archivos) y pone en marcha el servidor de socket. Finalmente, limpia un archivo de socket antiguo y comienza a escuchar activamente en \texttt{/tmp/nez\_watcher.sock}, indicando que el sistema está completamente operativo y listo para monitoriza archivos y recibir peticiones.

\begin{figure}[h!]
    \centering
    \includegraphics[width=\textwidth]{Diagramas UML/ss_descarga.png}
    \caption{Prueba de carga de un archivo por el Watcher.}
    \label{fig:watcher_upload}
\end{figure}

La Figura \ref{fig:watcher_upload} ilustra el flujo de operación principal del servicio. En la imagen se observa cómo, tras detectar la creación de un nuevo archivo en el directorio vigilado, uno de los \textit{workers} toma la tarea. A continuación, calcula el checksum SHA-256 del archivo para asegurar su integridad, registra los metadatos en MictlanX y, finalmente, procede con la carga del contenido. La correcta finalización de este proceso, visible en los logs, valida el núcleo funcional del sistema de ingesta y su capacidad para interactuar exitosamente con el VSS.

Una vez validado el flujo de carga, las pruebas se extendieron a los mecanismos de comunicación para la descarga de archivos. Se probó de manera exhaustiva el socket de dominio Unix \cite{POSIX:2018:ipc}, confirmando que el \textit{Watcher} podía recibir y procesar peticiones de otros procesos locales. Posteriormente, se integró el cliente desarrollado en C++ a este flujo. El objetivo era simular el caso de uso completo en el que una aplicación externa (el cliente) solicita la descarga de un archivo o expediente a través del socket. El éxito de estas pruebas confirmó que el \textit{Watcher} no solo actúa como un agente de ingesta, sino también como una puerta de enlace funcional para que otras partes del sistema accedan a los datos almacenados en MictlanX, preparándolo para su integración en un flujo de trabajo o \textit{deployer} más amplio.

\begin{figure}[h!]
    \centering
    \includegraphics[width=\textwidth]{Diagramas UML/ss_socket.png}
    \caption{Prueba de comunicación con el Watcher vía Socket Unix.}
    \label{fig:socket_request}
\end{figure}

Finalmente, la Figura \ref{fig:socket_request} demuestra la validez del mecanismo de IPC. En ella se aprecia cómo una aplicación cliente establece una conexión con el socket \texttt{/tmp/nez\_watcher.sock} y envía una petición para descargar un archivo específico. El texto de la petición es procesado por el servidor de socket del Watcher, que lo encola para que un \textit{worker} lo gestione. Esta prueba es fundamental, ya que justifica que el Watcher no es un servicio pasivo, sino que puede ser comandado por otras aplicaciones del ecosistema Nez, cumpliendo su rol como una pasarela de servicios de almacenamiento local.

\subsection{Problemas Encontrados y Correcciones}
Durante las primeras etapas de prueba del \textit{Nez-Daemon Watcher}, se identificó un problema recurrente relacionado con la carga de archivos al Espacio de Almacenamiento Virtual (VSS) de MictlanX. Aunque el servicio detectaba correctamente los nuevos archivos, las operaciones de subida fallaban intermitentemente.

Tras una investigación exhaustiva, se determinó que la causa raíz radicaba en la presencia de caracteres especiales en las "keys" (nombres de archivo o rutas relativas) que se generaban a partir de los archivos monitorizados. La API de MictlanX, al igual que muchos sistemas de almacenamiento, impone restricciones sobre los caracteres permitidos en los identificadores de objetos para garantizar la integridad y la compatibilidad. Los caracteres como espacios, barras invertidas, y ciertos símbolos, aunque válidos en un sistema de archivos local, provocaban errores en la interfaz del Router de MictlanX.

La solución implementada fue la adición de una función de "sanitización de keys" dentro del \textit{Watcher}. Esta función se encarga de procesar los nombres y rutas de los archivos, eliminando o reemplazando los caracteres no permitidos antes de enviar las "keys" a la API de MictlanX. Esta corrección aseguró que todas las "keys" generadas fueran compatibles con los requisitos del VSS, resolviendo así los fallos en la carga de archivos y mejorando significativamente la robustez del proceso de ingesta de datos.

\section{Conclusiones Finales}

\subsection{Conclusiones sobre el sistema desarrollado}
El desarrollo del \textit{Nez-Daemon Watcher} ha culminado con éxito, entregando un componente de software robusto y eficiente que cumple con todos los requerimientos funcionales y no funcionales establecidos. El sistema soluciona de manera efectiva el desafío de la interoperabilidad entre un sistema de archivos local y el Espacio de Almacenamiento Virtual (VSS) de MictlanX, automatizando el proceso de ingesta de datos de una manera desatendida y resiliente.

Las decisiones arquitectónicas tomadas durante el diseño demostraron ser acertadas en la práctica. La elección de \texttt{Python y su ecosistema asíncrono (asyncio)} fue idónea para una aplicación intensiva en operaciones de entrada/salida, permitiendo una alta concurrencia con un bajo consumo de recursos. La implementación de un \texttt{patrón productor-consumidor mediante una cola de tareas} fue fundamental para desacoplar la detección de eventos del procesamiento, otorgando al sistema la capacidad de absorber picos de trabajo y de recuperarse de fallos individuales sin afectar la operación general.

Asimismo, el uso de \texttt{Docker como tecnología de contenedorización} fue un pilar clave, no solo para simplificar el despliegue y la gestión de dependencias, sino también para garantizar la portabilidad y la consistencia del entorno, tal como lo dictan las mejores prácticas de la industria. El sistema abstrae exitosamente la complejidad del protocolo de carga en dos fases de MictlanX y, a través del \texttt{socket de dominio Unix}, se transforma de un simple agente de ingesta a una pasarela de servicios interactiva, capaz de ser comandada por otras aplicaciones locales.

En resumen, el \textit{Nez-Daemon Watcher} no es solo una herramienta funcional, sino también un ejemplo de cómo aplicar principios de diseño de software moderno —como la asincronía, la modularidad y la contenedorización— para resolver problemas complejos en el ámbito de los sistemas distribuidos y la ciencia de datos.

\subsection{Trabajos y perspectivas para el futuro}
Aunque el sistema actual es completo y funcional, su arquitectura modular abre la puerta a numerosas mejoras y líneas de trabajo futuro que podrían aumentar aún más su valor y capacidades.

\begin{itemize}
\item \textbf{Observabilidad Avanzada:} La monitorización actual, basada en el análisis de logs, es funcional pero limitada. Una mejora significativa sería la integración de un sistema de métricas con herramientas como \texttt{Prometheus}. Esto permitiría registrar y visualizar indicadores clave de rendimiento (KPIs) en tiempo real, como la cantidad de archivos procesados por segundo, la latencia de carga, el tamaño de la cola de tareas o la tasa de errores. Estos datos, visualizados en un panel de \texttt{Grafana}, ofrecerían una visión profunda del estado y rendimiento del servicio.

    \item \textbf{Gestión de Secretos Centralizada:} Para un entorno de producción con requisitos de seguridad más estrictos, la configuración y las credenciales no deberían gestionarse exclusivamente a través de variables de entorno. Se propone la integración con un sistema de gestión de secretos como \texttt{HashiCorp Vault} o servicios equivalentes en la nube. Esto centralizaría la gestión de información sensible, permitiría la rotación de credenciales y mejoraría la postura de seguridad general.

    \item \textbf{Ampliación de la API del Socket:} El socket de comunicación entre procesos (IPC) podría evolucionar hacia una API de control más rica. Se podría añadir la capacidad de consultar el estado de los \textit{workers}, obtener estadísticas de la cola de tareas, pausar o reanudar el monitoreo de archivos, o incluso actualizar ciertos parámetros de configuración en caliente, sin necesidad de reiniciar el servicio.

    \item \textbf{Escalado Dinámico de Workers:} Actualmente, el número de tareas \textit{worker} es un valor estático. Una futura versión podría implementar un mecanismo de autoescalado que ajuste dinámicamente el número de \textit{workers} en función de la carga de trabajo (por ejemplo, el número de tareas pendientes en la cola), optimizando así el uso de los recursos del sistema.

    \item \textbf{Interfaz de Usuario Web:} Para facilitar la administración y monitorización por parte de usuarios no técnicos, se podría desarrollar una sencilla interfaz de usuario web. Esta interfaz podría mostrar el estado del servicio, permitir la visualización y gestión del directorio de cuarentena, y ofrecer la posibilidad de iniciar manualmente operaciones de carga o descarga.
    
    \item \textbf{Sincronización Bidireccional:} El sistema podría evolucionar de un simple servicio de ingesta a una herramienta de sincronización completa. Esto implicaría no solo reaccionar a la creación de archivos, sino también a sus modificaciones y eliminaciones, propagando dichos cambios al VSS para mantener una réplica consistente del directorio local en la nube.
\end{itemize}
